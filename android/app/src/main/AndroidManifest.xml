<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!--
      F8.1: device calendar events are shown read-only. WRITE_CALENDAR is
      deliberately NOT declared — the app never creates or edits device
      events, and permission_handler only asks for calendar permissions that
      are declared here (PermissionUtils.getManifestNames), so the runtime
      request stays a read-only one.
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!--
      F7.3: birthdays are imported from the address book in a one-shot,
      read-only pass (Doğum günleri › "Rehberden aktar"). WRITE_CONTACTS is
      deliberately NOT declared — the app never creates, edits or deletes a
      contact, and permission_handler only asks for the contacts permissions
      declared here (PermissionUtils.getManifestNames), so the runtime request
      stays a read-only one. Only the name and the date are kept; the contact
      id, photo, phone numbers and e-mails are never fetched or stored.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH" />
            </intent-filter>
        </activity>

        <!-- Ana ekran widget'ları (F5.1). Liste, F5.1 öncesi tek widget'ın
             sınıfını korur; yerleştirilmiş widget'lar Liste olarak sürer. -->
        <receiver
            android:name=".ReminderTodayWidgetProvider"
            android:exported="true"
            android:label="@string/widget_today_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_today_widget_info" />
        </receiver>

        <receiver
            android:name=".ReminderListWidgetProvider"
            android:exported="true"
            android:label="@string/widget_list_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_list_widget_info" />
        </receiver>

        <receiver
            android:name=".ReminderNextWidgetProvider"
            android:exported="true"
            android:label="@string/widget_next_label_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_next_widget_info" />
        </receiver>

        <receiver
            android:name=".ReminderQuickAddWidgetProvider"
            android:exported="true"
            android:label="@string/widget_quick_add_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_quick_add_widget_info" />
        </receiver>

        <!-- Liste koleksiyonu, API 26–30 (API 31+ RemoteCollectionItems). -->
        <service
            android:name=".ReminderListWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- Liste satırlarının tek tıklama şablonu (tamamla / aç). -->
        <receiver
            android:name=".ReminderWidgetClickReceiver"
            android:exported="false" />

        <!-- Widget'ların saate bağlı yenilemesi (uyandırmayan esnek alarm) ve
             saat / saat dilimi değişimi. -->
        <receiver
            android:name=".ReminderWidgetRefreshReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- flutter_local_notifications: bildirim aksiyonları (Tamamla/Ertele,
             F3.2) bu receiver üzerinden arka plan isolate'ine iletilir. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- native_geofence: geofence olaylarını WorkManager + arka plan
             Flutter engine ile Dart callback'ine iletir (uygulama kapalıyken de).
             Geofencing PendingIntent'i uygulamanın kendi kimliğiyle gönderilir,
             bu yüzden receiver dışa açık olmak zorunda değil. -->
        <receiver
            android:exported="false"
            android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceBroadcastReceiver" />

        <!-- native_geofence: Android reboot sonrası geofence'leri silir; kayıtlı
             bölgeler burada yeniden oluşturulur. Uygulama güncellemesinde de
             güvenlik için yeniden kaydedilir. -->
        <receiver
            android:exported="false"
            android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceRebootBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
